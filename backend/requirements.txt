# Pipecat AI Backend Requirements
# Optimized for local AI rig with Ollama and Cartesia

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

# Pipecat AI Framework (install from parent directory)
# Run: pip install -e ..

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database (SQLite for simplicity, can upgrade to PostgreSQL)
# sqlalchemy>=2.0.0
# alembic>=1.12.0

# Logging
loguru>=0.7.0

# Environment Variables
python-dotenv>=1.0.0

# HTTP Client
httpx>=0.25.0
aiohttp>=3.9.0

# Audio Processing (if needed for additional features)
# numpy>=1.24.0
# scipy>=1.11.0

# Development Dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
