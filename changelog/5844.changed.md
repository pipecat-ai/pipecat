- Changed `SarvamTTSService` to reject sample rates Sarvam's streaming surfaces don't accept — anything outside 8000, 16000, 22050, and 24000 Hz — when the service is constructed, instead of connecting and failing once synthesis starts.
