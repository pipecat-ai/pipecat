- Fixed `SimliVideoService` not producing avatar video until the first TTS audio frame arrived. The video consumer no longer waits on the audio resampler event (which it never uses), so video starts streaming as soon as the connection is established, avoiding missing video and unbounded growth of the incoming video queue when TTS is delayed or absent.
