Fixed `OpenAIResponsesLLMService` not applying `system_instruction` updates sent via `LLMUpdateSettingsFrame` on subsequent turns. The `previous_response_id` optimization caused OpenAI's Responses API to ignore the changed `instructions` field. The service now clears its `previous_response_id` state whenever `system_instruction` changes, forcing the next call to send fresh instructions.
