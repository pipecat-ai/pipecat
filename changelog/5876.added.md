- Added an optional `response_schema` argument to `LLMService.run_inference()`, a JSON schema the reply must follow. OpenAI, Anthropic and Google have the provider enforce it, so the reply is JSON text in that shape. Bedrock cannot enforce one and ignores it with a warning.

  ```python
  reply = await llm.run_inference(context, response_schema=schema)
  ```
