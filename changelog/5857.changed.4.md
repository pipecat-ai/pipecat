- A judge over an LLM now asks it to classify, with the conversation as structured state and the outcomes as options, in place of the previous prose prompt, and judges a simulation one bot turn at a time rather than the whole run in one call. A borderline reply can be judged differently than before, and a simulation costs a call per bot turn plus one for the goal.
