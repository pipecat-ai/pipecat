- Simplified Inworld bidirectional TTS behavior during WebSocket reconnects:
  - On reconnect, the service now discards the previous synthesis context and starts a fresh one (instead of retrying to resume the old context).
