- Added simple `LiveKitWorkerRunner` to `pipecat.runner.livekit_worker` to support deploying Pipecat bots as LiveKit Agents (Workers).
- Updated `LiveKitTransport` to support dependency injection of an existing `rtc.Room` object.
