- `OpenAIResponsesLLMService` (WebSocket) now waits at most one second for a cancelled response's remaining events before replacing the connection, and sends the next request with the full context instead of chaining from a response the server no longer holds. The drain used to last as long as the rest of the abandoned reply took, and when it timed out the abandoned reply could be delivered as the next turn's answer.
