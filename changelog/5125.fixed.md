- Fixed TTS providers that return a multi-word timestamp token with punctuation repeated from the preceding token causing the clause to be emitted once as passthrough and again during force-completion, duplicating assistant context and transcripts. Repeated boundary punctuation is now removed only when it matches the already-consumed source boundary and the remaining token matches the expected text.
