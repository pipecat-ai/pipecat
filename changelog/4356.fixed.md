- Fixed RTVI `send-text` interrupting but not producing a new response in speech-to-speech pipelines. `OpenAIRealtimeLLMService` and `AWSNovaSonicLLMService` now forward typed user text to the provider (via a `conversation.item.create` + `response.create` pair and an interactive user text event, respectively), and `RTVIProcessor` emits an `InputTextRawFrame` alongside the existing `LLMMessagesAppendFrame` so realtime services receive the typed turn through the same path they already use for audio input.
