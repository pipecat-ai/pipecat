- The turn-completion evals run 20 models at once and give an expectation without its own `within_ms` a 30 s timeout (`-t 30` in `run.sh`), and the manifest drops three models that time out on nearly every run.
