- The turn-completion evals run 32 models at once and give an expectation without its own `within_ms` a 30 s timeout (`-t 30` in `run.sh`), and the manifest drops three models that time out on nearly every run, plus its second Ollama entry, since the local Ollama runs the judge's explainer and takes one model at a time.
