- Fixed OpenAI-compatible LLM services passing a Gemma model's `<channel|>` tokens on as text. A server such as Ollama can let the token through as content where spoken text is followed by a tool call, so the bot spoke it or a `<channel|>thought` section that followed. For a Gemma model, `BaseOpenAILLMService` now drops the switch and the channel section it opens.
