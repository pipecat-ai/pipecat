- `OpenAIResponsesLLMService` bounds stalled WebSocket response streams with `stream_idle_timeout_secs` (20 seconds by default, `None` to disable). A timeout reports an error, ends the response without replaying it, and closes the connection so the next turn cannot receive stale events.
