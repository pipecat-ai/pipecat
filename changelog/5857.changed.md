- The release evals and the turn-completion evals judge with Jev, with `gemma4:12b` on Ollama as the explainer. Running either needs `TYPESAFE_API_KEY` exported in the shell, since the harness doesn't read `.env`; each README explains how to judge with Ollama alone.
