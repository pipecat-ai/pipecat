- Added an `effect` setting to `AzureTTSService` and `AzureHttpTTSService`, which
  passes Azure's `<voice effect>` audio effect processor (`eq_car`,
  `eq_telecomhp8k`) through to the synthesis request.
