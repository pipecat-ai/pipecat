- Added an opt-in `--eval` flag to `pipecat create` (and an `Enable evals?` wizard prompt, off by default) that scaffolds an `"eval"` entry in the bot's `transport_params`, so the generated bot is runnable with `-t eval` for behavioral evals without any manual edit. The entry mirrors the bot's audio/video settings and is inert unless the bot is run with `-t eval`.
