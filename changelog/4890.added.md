- Added `AudioBufferStartRecordingFrame` and `AudioBufferStopRecordingFrame` control frames. Pushing them through the pipeline starts and stops `AudioBufferProcessor` recording from within the frame flow, so any upstream processor (RTVI message, function call, etc.) can control recording without holding a reference to the processor. The existing `start_recording()` / `stop_recording()` methods continue to work.
