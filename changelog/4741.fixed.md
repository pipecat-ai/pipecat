- Fixed duplicate spoken responses when using `FilterIncompleteUserTurnStrategies`. The turn-completion mixin now speaks at most one `✓` completion per user turn, so multiple inferences within a single turn (a Smart Turn `COMPLETE` flicker, an incomplete-timeout re-prompt) no longer make the bot repeat the same response. The latch resets when a new user turn starts (`UserStartedSpeakingFrame` / `InterruptionFrame`).
