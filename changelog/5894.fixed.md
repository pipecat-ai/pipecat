- Fixed numbers reaching the assistant context split into fragments ("2 5 0", "20 26") with `InworldTTSService`, which reports the digits of a number as separate word-timestamp tokens and can split them across messages. `merge_punct_tokens` now merges a number's digit tokens, and `TTSService.add_word_timestamps(..., pre_merge_tokens=True)` holds a trailing number until the next message for the same context or until the context ends.
