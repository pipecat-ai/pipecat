- `ElevenLabsDialogueTTSService` closes a Text-to-Dialogue context as soon as its turn ends, freeing the server-side context slot sooner. The endpoint generates whatever text the context still holds before emitting `is_final`, so the turn reaches the client in full.
