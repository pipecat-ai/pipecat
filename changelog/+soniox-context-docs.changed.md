- Corrected the `SonioxSTTService` context documentation: `context` takes either a
  `SonioxContextObject` or a plain string, on every current Soniox model. The old
  wording tied the two forms to retired models via "context_version" numbering that
  Soniox no longer publishes.
