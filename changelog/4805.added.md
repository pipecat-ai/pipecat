- Added an `http_client` parameter to `OpenAITTSService` and Whisper-based STT services (`BaseWhisperSTTService`, `OpenAISTTService`, `GroqSTTService`), allowing a custom `httpx.AsyncClient` — e.g. with explicit timeouts for high-latency environments — to be used for API requests.
