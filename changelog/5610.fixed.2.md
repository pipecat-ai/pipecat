- `DeepgramFluxSTTService` now classifies its one-shot connect handshake the way `DeepgramSTTService` does: a 4xx rejection (bad key, exhausted credit) marks the service unusable so failover can act, while connect failures without an HTTP status stay transient.
