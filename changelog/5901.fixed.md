- Fixed `expand_percentages` splitting numbers with thousand separators, so "1,200%" now reads as "one thousand, two hundred percent" instead of "1,two hundred percent". Fractional digits are now read one at a time like `expand_numbers`, so "1.10%" reads as "one point one zero percent" instead of "one point one percent".
