- The websocket transports (`fastapi`, `server`, `client`) now pace audio output even when the serializer emits no payload for a frame. A stream resampler accumulates across calls and returns nothing for most of them, so output drained faster than playout and `BotStoppedSpeakingFrame` fired early whenever the pipeline sample rate differed from the wire rate.
