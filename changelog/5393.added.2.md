- Added `max_consecutive_zero_audio_contexts` to `FishAudioTTSService`, defaulting to 1 rather than the `TTSService` default of 3. Fish Audio answers a request naming a voice it doesn't know with silence rather than an error, so a single silent context already says the service can't speak.
