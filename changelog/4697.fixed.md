- Fixed `pipecat init` generating an `env.example` where inline comments on optional variables (e.g. `OPENAI_MODEL`, `CARTESIA_VOICE_ID`) were parsed by python-dotenv as the variable's value, causing the generated bot to fail on startup. Comments now sit on their own line.
