- An async function call's result is now reported even when the turn it arrived on was interrupted, or when it arrived while the user was speaking — cases where the inference that would have delivered it never ran at all, leaving the result sitting in the context with nothing drawing the model's attention to it. The bot reports it in a reply of its own, once it has gone quiet and the user is silent, rather than appending it to an answer about something else.
