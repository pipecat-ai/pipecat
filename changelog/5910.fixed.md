- Fixed the Gemini adapter appending the system instruction as an extra user message after a vision turn. A user message carrying an image (with or without text) was not counted as a regular message, so a context holding only such turns re-sent the system prompt as the conversation's final user message while it was also sent as the `system_instruction` parameter.
