- Fixed `GatedLLMContextAggregator` forwarding `StartFrame` downstream twice: the `StartFrame` branch fell through to the catch-all `else`, which pushed the frame a second time. The branch now belongs to the same if/elif chain as the other frame types.
