- `FunctionCallResultFrame` and `FunctionCallInProgressFrame` are now system frames. All function call lifecycle frames (`FunctionCallsStartedFrame`, `FunctionCallInProgressFrame`, `FunctionCallResultFrame`, `FunctionCallCancelFrame`) are now system frames to ensure they are processed immediately and maintain FIFO ordering with each other.
