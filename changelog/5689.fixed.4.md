- Fixed built-in tools (the `cancel_<tool>` tools) never reaching the provider when the context advertises no tools, and the realtime services (OpenAI, Grok and Inworld Realtime, Gemini Live, Nova Sonic) dropping their init-provided tools when the context's only tools were built-in ones. Built-in tools are now sent on every inference beside whatever tools the context or the service provides.
