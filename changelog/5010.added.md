- Added `WhisperStreamingSTTService`, a local Whisper STT service that pushes live interim transcriptions from a small model while the user speaks and produces the final transcript with a larger model.
