- Fixed `OpenAIResponsesLLMService` and `OpenAIResponsesHttpLLMService` ignoring a context's `tool_choice`, whether set on `LLMContext` or at runtime with `LLMSetToolChoiceFrame`. It now reaches the Responses API the way it already reached Chat Completions. The string options carry over unchanged; the object forms are translated, since Chat Completions nests a chosen tool's name one level deeper than the Responses API does.
