- A `pipecat eval suite` manifest entry can carry an `env:` mapping, added to the spawned bot's environment over the suite's own, so a bot that reads its configuration (a model, a reasoning effort) from the environment can run under several configurations as separate entries, each with its own `name:`. `EvalRun` gains `env`.
