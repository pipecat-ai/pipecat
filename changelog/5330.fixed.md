- `VoicemailDetector` now lets `EndWorkerFrame` through after a voicemail verdict, so the documented `on_voicemail_detected` handler can end the call. Closed classifier and conversation gates already admitted `CancelWorkerFrame` (a system frame) but dropped other worker lifecycle frames.
