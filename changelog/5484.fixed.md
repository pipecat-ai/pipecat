- Fixed the start of a MoQ bot's first utterance being dropped on cold starts: `MOQOutputTransport` now waits for the peer to subscribe to the bot's audio track before reporting ready (up to `MOQParams.audio_out_subscriber_timeout`, 15s). Requires `moq-rs` 0.4.6.
