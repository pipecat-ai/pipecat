- Fixed `DeepgramFluxSTTService` and `DeepgramFluxSageMakerSTTService` reporting themselves usable again without reconnecting when settings were updated while the connection was down. A settings update makes a service usable again, since the new settings may be the fix, so Flux now rebuilds the connection when there is none to send a `Configure` message on. This covers the case where the corrected setting is the one that caused the connection to be refused.
