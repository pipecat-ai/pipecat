- Removed `AICVADAnalyzer` and `AICFilter.create_vad_analyzer()`, deprecated
  since 1.4.0. They read the enhancement model's energy-based VAD, which
  `aic-sdk` 3.0 removed. Use `AICFilterVADAnalyzer` for enhancement plus
  detection, or `AICQuailVADAnalyzer` for detection alone.
