- Fixed `OpenAILLMService` (and services built on it) producing an empty `tool_call_id` when a provider streams the tool-call `id` and `function.name` in separate deltas. The id is now captured independently of the name delta.
