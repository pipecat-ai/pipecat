- Fixed `GoogleSTTService` dropping the user's last utterance every time it restarts its stream at Google's four minute streaming limit. The response loop stopped on the limit before handling the response it had already received, so the final Google sends when the request stream half-closes was discarded. The loop now runs until Google closes the response stream, and the restart is unchanged.
