- Fixed a race condition where `asyncio.CancelledError` escaping from `InterruptionFrame` processing could permanently kill the `FrameProcessor` input handler, causing the entire pipeline to stall silently. Added CancelledError protection in `__input_frame_task_handler`, `_start_interruption`, and zombie process task detection in `__cancel_process_task`.
