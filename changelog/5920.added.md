- Added `TransportParams.audio_out_filter`, a `BaseAudioFilter` the output transport runs on outgoing audio before the mixer and the write. Like `audio_in_filter`, it starts on setup, stops on cleanup and receives `FilterEnableFrame` and `FilterUpdateSettingsFrame`. Audio the transport generates itself (mixer audio, end silence, DTMF tones) is not filtered.
