- Fixed `SmallWebRTCTransport` importing OpenCV even for audio-only pipelines. `cv2` is now imported lazily, only when a non-RGB video frame actually needs converting. The `webrtc` extra's OpenCV dependency also switched from `opencv-python` to `opencv-python-headless`, avoiding GUI system library requirements.
