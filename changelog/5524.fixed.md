- Fixed the Fireworks function-calling example, which named
  `accounts/fireworks/models/gpt-oss-20b`. Fireworks retired that model from its
  serverless endpoint, so the example now uses
  `accounts/fireworks/models/nemotron-3-ultra-nvfp4`.
