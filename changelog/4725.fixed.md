- Fixed `AWSBedrockLLMAdapter` raising `UnboundLocalError` when a user message places its single image before the text (e.g. a `UserImageRawFrame` followed by a prompt). The image-before-text reorder step now only moves the image when it actually follows the text, so image-first content is converted correctly instead of being dropped.
