- Fixed an issue where auto context summarization would trigger a summarization request on every turn that could never make progress, such as when the initial system message alone exceeded `max_context_tokens` or when only preserved messages (the system prompt, the last `min_messages_after_summary` messages, unresolved function call sequences) remained. The trigger now skips, and warns once, when there is nothing to summarize.
