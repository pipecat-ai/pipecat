- Fixed an issue where auto context summarization would trigger on every turn
  when the initial system message alone exceeded `max_context_tokens`. The
  token trigger now counts only the summarizable context (excluding the
  preserved initial system message, which summarization never compresses), and
  warns once when the system message alone meets or exceeds the limit.
