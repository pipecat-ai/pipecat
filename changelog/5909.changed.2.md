- The bus event names of the client's snapshot and its cancel request are public: `UI_SNAPSHOT_EVENT_NAME` and `UI_CANCEL_JOB_GROUP_EVENT_NAME` in `pipecat.bus.ui`, for a worker that handles bus messages itself or a client that speaks the protocol.
