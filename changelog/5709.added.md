- Added `VuiTTSService`, a local streaming TTS service for [Vui Nano](https://github.com/fluxions-ai/vui) (219M active parameters, Apache 2.0) running in-process on CUDA or Apple Silicon (MLX) via the `vui-tts` package. Install with `pip install "pipecat-ai[vui]"` (Python 3.12).
