- Added `LLMAssistantAggregator.has_blocking_function_calls_in_progress`: whether a synchronous function call is in flight, one whose result the model waits for before it runs again, as opposed to an asynchronous call (`cancel_on_interruption=False`) that has its placeholder in the context.
