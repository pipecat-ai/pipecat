- Fixed `SpeechmaticsSTTService` raising `AttributeError` when constructed with an English locale it has no output-locale mapping for, such as `Language.EN_IN`. It logs a warning and falls back to the base language code in that case, and the warning named the service — which has no name yet, since the connection config is built before the base class initializes.
