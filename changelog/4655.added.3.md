- Added a `notify_client_disconnected` parameter to `WebsocketServerParams` (default `True`). Set it to `False` to keep the server and the bot's pipeline running when a client disconnects, so one bot can serve several sequential connections instead of cancelling on the first disconnect.
