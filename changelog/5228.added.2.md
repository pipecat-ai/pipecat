- Async tool calls now announce themselves. With a response strategy configured, an async (`cancel_on_interruption=False`) tool's final result routes through it automatically: the result lands in the LLM context right away, and the spoken announcement waits for the next opening — no announcement code in the tool. Results that finish close together are announced together, as one response rather than a chain.
