- Websocket-based services now bound the websocket closing handshake at 2 seconds instead of the `websockets` default of 10. A service disconnects while handling the `EndFrame`, before the frame continues downstream, so a peer that never acknowledges the close delayed pipeline shutdown by that much per service. The bound is configurable per service via the new `ws_close_timeout` argument on `WebsocketService`.
