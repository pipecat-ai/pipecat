- Fixed `PatternPairAggregator` handling of identical opening and closing delimiters, such as Markdown code fences, so surrounding text is preserved, removed content stays suppressed across chunks, and handlers run once per completed pair.
