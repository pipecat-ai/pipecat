- ⚠️ Auto-switching to external user turn strategies for realtime services has moved onto the service-metadata recommendation mechanism and is no longer conditioned on `realtime_service_mode`. A realtime service recommends `ExternalUserTurnStrategies` via its metadata frame; a user-supplied `user_turn_strategies` still wins.
