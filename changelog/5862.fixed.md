- Fixed observer self-removal leaving its proxy task running and processing pending events. The current callback finishes before the proxy exits, including when the same observer is immediately reattached.
