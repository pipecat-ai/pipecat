- Bots scaffolded by `pipecat init` with Sarvam, Whisper, or Moonshine STT, Sarvam TTS, or Google STT no longer crash at startup. Their generated code now uses each service's default model (or location, for Google STT) instead of reading an environment variable the generated `.env.example` never listed. The unset variable arrived as an explicit `None`, which overrode the working default and failed validation.
