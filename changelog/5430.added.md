- Added a `normalization` field to `CartesiaTTSService.Settings` and
  `CartesiaHttpTTSService.Settings`, passing Cartesia's text normalization mode
  (`"auto"`, `"off"`, or a locale code such as `"en-GB"`) through to synthesis
  requests.
