- `LLMWorker` and `LLMContextWorker` no longer require a `name`. One is generated when omitted, as for any other worker; a worker that others address by name, such as one in another process, still needs one.
