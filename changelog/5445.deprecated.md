- Deprecated `LatencyBreakdown.chronological_events()`. Use `LatencyBreakdown.contribution_lines()` instead, which names every part of the interval rather than the services that happened to report a metric, and whose lines sum to the measured latency. Will be removed in 2.0.0.
