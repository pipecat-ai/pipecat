- Added an optional `@tool_options(cancel_on_interruption=..., timeout_secs=...)` decorator to override a direct function's call options. Options resolve by precedence: an explicit argument to `register_direct_function()` wins, otherwise the decorator value, otherwise the default — so the decorator applies whether the function is registered explicitly or auto-registered from an `LLMContext`.
