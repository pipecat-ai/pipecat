- Added `thinking` to `DeepSeekLLMService.Settings`, which controls DeepSeek's
  thinking mode: `settings=DeepSeekLLMService.Settings(thinking={"type": "disabled"})`
  turns off the reasoning pass that V4 models run by default.
