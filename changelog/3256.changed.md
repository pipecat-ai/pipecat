- Updated docstrings
- Added `enable_word_timestamps` as a parameter the client can set with default as false
- Updated websockets implementation to better flush and close context to handle long inputs