- Fixed `DailyTransport` stopping a Daily room's transcription when the bot joins. Daily reports `transcription-started` for every transcription in a room, including ones started by the room or another participant, and the transport replied with its own participant list. That list is empty when `transcription_enabled` is False, and an empty list tells Daily to transcribe nobody. The transport now leaves transcriptions it was not asked to manage alone.
