- Fixed `InterruptionFrame` discarding clock-queued `TTSTextFrame`s whose presentation time had already passed, which left `LLMAssistantAggregator._aggregation` empty for interrupted bot turns and produced an empty `on_assistant_turn_stopped.message.content` for words the caller had already heard. The output transport now drains elapsed-PTS frames downstream before the interrupt cancels the clock queue.
