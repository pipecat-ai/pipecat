- Added a `kind` field to `LLMMarkerFrame` naming what the marker means, which the turn-completion mixin sets to `complete`, `short` or `long`, and a `bot-llm-marker` RTVI message that `RTVIObserver` sends when its new `bot_llm_marker_enabled` parameter is on (off by default).
