- Fixed `TranscriptionFrame` being dropped when queued behind the frame that starts a user turn. The frame is now uninterruptible, so transcripts arriving in the same tick (e.g. interim + final, back-to-back finals, or speech + keypad entry) survive the turn's own interruption.
