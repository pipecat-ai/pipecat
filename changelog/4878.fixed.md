- Fixed `DeepgramSTTService` freezing when `send_media()` blocked indefinitely on a stuck WebSocket connection. Audio is now sent via a dedicated queue/sender task so the pipeline never stalls waiting on Deepgram. A watchdog task detects when sends stop progressing and triggers a reconnect, or sends a KeepAlive when the connection is idle.
