- **Behaviour change:** the default Grok Realtime voice is now `eve`, matching xAI's own server-side default, instead of `Ara`. Anyone relying on the previous out-of-the-box voice should set `voice="ara"` explicitly on `SessionProperties`.
