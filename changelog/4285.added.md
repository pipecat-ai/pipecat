- Added MCP resource support to `MCPClient`: `list_resources()`, `read_resource()`, and `read_all_resources()` methods for fetching read-only data from MCP servers. Includes `MCPResource` and `MCPResourceList` dataclasses.
