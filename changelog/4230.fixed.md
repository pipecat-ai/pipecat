- Fixed duplicate LLM replies that could occur when multiple async function call results arrived while an LLM request was already queued. Context frames now carry a snapshot of the message list at the time they are created, so each LLM invocation sees only the messages that were present when it was triggered.
