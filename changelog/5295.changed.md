- `SonioxTTSService` now defaults to Soniox's `tts-rt-v2` model, with `Bryce` as the default voice. `tts-rt-v2` uses the same real-time WebSocket API as `tts-rt-v1`, so moving over needs no code changes — though a `voice` set explicitly must be one `tts-rt-v2` offers.
