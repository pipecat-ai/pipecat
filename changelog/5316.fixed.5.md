- The `DeprecationWarning` for reading a deprecated `StartFrame` or `FrameProcessorSetup` field is reported once per call site, and points at the line that performed the read. A caller that reflects over every dataclass field of every frame it sees — a frame serializer, for example — previously repeated the warning without bound.
