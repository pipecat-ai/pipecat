- Improved audio context management in `AudioContextTTSService` by moving context ID tracking to the base class and adding `supports_concurrent_contexts` parameter to control concurrent TTS request handling.
  - Added helper methods: `has_active_audio_context()`, `get_active_audio_context_id()`, `remove_active_audio_context()`, `reset_active_audio_context()`
  - Simplified Cartesia, ElevenLabs, Inworld, Rime, and AsyncAI TTS implementations by removing duplicate context management code
