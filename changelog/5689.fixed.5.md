- Fixed a delegation the frontend abandons mid-stream, by cancellation or an error while handling the backend's output, leaving the backend at work: its job is now cancelled at once, and `BackendLLMWorker` interrupts its pipeline on a cancelled job, so the next delegation neither waits behind the old one nor receives its output.
