- `VoicemailDetector` no longer swallows `EndWorkerFrame` travelling upstream through a closed gate, so a handler that ends the call reaches `PipelineWorker`. Closed classifier and conversation gates already admitted `CancelWorkerFrame` (a system frame) but dropped other worker lifecycle frames.
