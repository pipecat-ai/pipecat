- Added a `conversation_span` property to `TurnTraceObserver`, exposing the open OpenTelemetry conversation span. `additional_span_attributes` only covers attributes known when the span is created, so this lets integrations set trace-level attributes that are learned late in a call, such as a reference to a call recording uploaded once the call ends. The property returns `None` before the conversation starts and after it ends.
