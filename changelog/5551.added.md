- Added `speaker_id`, `length_scale`, `noise_scale` and `noise_w_scale` settings
  to `PiperTTSService` and `PiperHttpTTSService`, exposing Piper's per-request
  speaker selection, speaking speed and variability controls.
