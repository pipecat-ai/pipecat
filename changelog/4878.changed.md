- ⚠️ `DeepgramSTTService` now requires `deepgram-sdk>=7.0.0`. The minimum supported version has been raised from `6.1.1` to `7.0.0`. Update your dependency with `uv add "pipecat-ai[deepgram]"` or pin `deepgram-sdk>=7.0.0,<8` directly.
