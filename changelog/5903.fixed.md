- `OpenAIResponsesLLMService` (WebSocket) with `retry_on_timeout` now keeps the retry window open until the response produces output. The window used to close on any event after `response.created`, and the server sends `response.in_progress` right after it, so a response that then stayed silent was never re-issued.
