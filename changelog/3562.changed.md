- Added support for TTFS `finalize` attribute in `SpeechmaticsSTTServce` and set the default mode to `EXTERNAL` to support Pipecat-controlled VAD.
- Changed dependency to `speechmatics-voice[smart]>=0.2.8`
