- Fixed `OpenAIResponsesLLMService` (WebSocket) going silent with no error when a reasoning-capable model spent its entire output budget on an internal reasoning item and produced no visible text or function call. This is now detected and retried once with the full conversation context; if the retry also produces no output, the error now surfaces via `on_error` instead of silently returning nothing.
