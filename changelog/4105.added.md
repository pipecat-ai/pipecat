- Added `exclude_from_summary` support for context summarization. Messages with `"exclude_from_summary": True` are preserved intact through summarization instead of being compressed into the summary. Also available via `FunctionCallResultProperties.exclude_from_summary` to exclude entire function call groups (assistant message + all tool responses) from summarization.
