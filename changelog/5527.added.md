- Added a `version` field to `DeepgramSTTService.Settings` (and
  `DeepgramSageMakerSTTService.Settings`), pinning transcription to a specific
  Deepgram model version instead of whichever one `latest` currently resolves to.
  Previously reachable only as an untyped `extra` key, which continues to work.
