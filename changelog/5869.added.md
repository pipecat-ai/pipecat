- `LLMClassifier` takes a `timeout` (default 10 s) and raises `ClassifierError` when the LLM does not answer in time, or when the LLM call fails for any other reason, so whoever waits on the classifier always gets an answer or a `ClassifierError`. `JevClient` already had one.
