- `LLMClassifier` takes a `timeout` (default 10 s) and raises `ClassifierError` when the LLM does not answer in time, so a hung request never holds up whoever is waiting on the classifier. `JevClient` already had one.
