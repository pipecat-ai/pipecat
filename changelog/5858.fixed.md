- Fixed `MistralSTTService` sending audio on a connection the server had already closed, which raised instead of dropping the chunk when a reconnect failed. Its teardown now also releases such a connection and emits `on_disconnected`.
