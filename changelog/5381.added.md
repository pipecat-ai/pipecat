- Added an AWS Bedrock Mantle example (`examples/voice/voice-aws-mantle.py`) showing how to reach Mantle's OpenAI-compatible Chat Completions API through the existing `OpenAILLMService` by setting `base_url` and `api_key`. No new AWS Bedrock service was introduced; `AWSBedrockLLMService` (the native Bedrock Converse API integration) is unchanged.
