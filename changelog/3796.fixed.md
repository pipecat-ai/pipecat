- Fixed race conditions in idle processors (`IdleFrameProcessor`, `UserIdleProcessor`) where `asyncio.Event` signals set during async timeout callbacks were unconditionally cleared by a `finally` block, causing false-positive idle detection.
