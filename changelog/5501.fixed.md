- Fixed `EvalTranscriber` handing raw PCM to segmented STT services that take
  their segments as WAV containers (the `wants_wav_segments` contract, true of
  the HTTP services). In a pipeline `SegmentedSTTService` does that wrap;
  calling `run_stt()` directly now does it too, so HTTP STT services work as
  bot-audio transcribers the way `pipecat.evals.transcribe` documents.
