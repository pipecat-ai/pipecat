- Fixed `GeminiLiveLLMService` hitting `1007` on the first audio turn after a text-only prelude when `inference_on_context_initialization=False` on Gemini 2.5. The deferred turn-complete workaround was only cleared on the audio user-stop path, so a stale `client_content(turn_complete=True)` raced with `realtime_input.audio`. It now also clears on `_send_user_text` and `_create_single_response`.
