- `DeepgramFluxSTTService`, `DeepgramFluxSageMakerSTTService` and `CartesiaTurnsSTTService` now push an `EagerEndOfTurnTranscriptionFrame` when they predict an end of turn and an `EagerEndOfTurnCancelFrame` naming that prediction when they withdraw it, which `EagerUserTurnStrategies` acts on.
