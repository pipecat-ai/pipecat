- `DeepgramFluxSTTService`, `DeepgramFluxSageMakerSTTService` and `CartesiaTurnsSTTService` push an `EagerTranscriptionFrame` when they predict an end of turn and an `EagerEndOfTurnCancelFrame` naming that prediction when they withdraw it, which `EagerUserTurnStrategies` acts on. Both frames are emitted only while `enable_eager_end_of_turn` is on.
