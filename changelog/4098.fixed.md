- Fixed a race condition in `TurnAnalyzerUserTurnStopStrategy` where a non-finalized `TranscriptionFrame` arriving after the STT timeout expired would never re-check turn stop conditions, causing the turn to stall for up to 5 seconds (the `user_turn_stop_timeout` safety-net).
