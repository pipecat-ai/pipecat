- Fixed TTS usage metrics going unreported in `TextAggregationMode.TOKEN`, where streamed text is counted once per utterance rather than once per token and only the end of an LLM response settled that count. A `TTSSpeakFrame`, a response the user interrupted, and a session torn down with a `CancelFrame` now each report the text they accumulated for synthesis, and a `TTSSpeakFrame` reports separately from the LLM turn it follows or interrupts.
