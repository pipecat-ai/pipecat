- Fixed the start of user speech being clipped from transcripts when `GeminiLiveLLMService` is configured for locally-driven turns (server-side VAD disabled). Audio captured before local turn detection confirms the turn was reaching the service ahead of `activity_start` and getting discarded as out-of-window; the service now replays a short pre-roll of that audio right after `activity_start`, so the onset is preserved.
