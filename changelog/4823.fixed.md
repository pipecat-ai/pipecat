Fixed an `LLMService` regression where re-advertising a tool with the same name
but a different handler (for example, a per-node handler in Pipecat Flows)
silently kept the previous handler bound. Auto-registered handlers are now
rebound when the advertised handler changes, while explicit `register_function`
registrations are still left untouched.
