- `GeminiLiveLLMService` now defaults to `models/gemini-3.8-live`, where it defaulted to `models/gemini-2.5-flash-native-audio-preview-12-2025`. Synchronous tools are declared `BLOCKING` on 3.8, where `NON_BLOCKING` is the model's default, so a tool call still finishes the model's turn.
