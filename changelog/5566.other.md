- Documented `SonioxSTTService`'s `context` setting as accepting either a `SonioxContextObject` or a plain string of background text on any current Soniox model, along with the 8k-token cap Soniox applies to the context object.
