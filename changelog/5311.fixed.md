- Fixed `UserBotLatencyObserver` silently dropping turns started without VAD (for example by `TranscriptionUserTurnStartStrategy`). The observer now resets per-cycle state on `UserStartedSpeakingFrame` and anchors the latency measurement at `UserStoppedSpeakingFrame` when no `VADUserStoppedSpeakingFrame` was observed, so transcription-started turns still emit `on_latency_measured` and `on_latency_breakdown`.
