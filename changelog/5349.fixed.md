Fixed transcript loss for sentences whose TTS and user-facing texts are byte-identical but contain markup (e.g. Cartesia's `<spell>` tag): they were held atomically as one whole-sentence segment, so an interruption before the final word dropped every word already spoken from the accumulated user-facing text. Byte-identical segments now advance incrementally via the exact identity mapping.
