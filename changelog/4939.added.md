- Added `MCPClient.tools()`: `LLMContext(tools=await mcp.tools())` is now all you need to use MCP tools — connecting, tool registration, and closing the connection at pipeline end are automatic. Also added `tools_arguments`, to pin tool arguments the model shouldn't control, and `call_tool()`, to call an MCP tool directly.
