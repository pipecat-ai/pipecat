- The pipeline clock now runs from the moment the pipeline starts setting up rather than from the `StartFrame`. Processors connect while they are set up and push frames as they do, and those frames were timestamped zero. Frame presentation timestamps therefore no longer start near zero, they start at roughly the time setting up took; everything comparing them does so relatively, so pacing and playback are unaffected.
