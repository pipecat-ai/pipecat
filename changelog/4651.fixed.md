- Fixed importing any LLM service failing with a raw `ImportError` when the optional `websockets` package wasn't installed. Core `llm_service.py` had begun importing `websockets` unconditionally, short-circuiting the guard that points users at the right `pipecat-ai[...]` extra (e.g. `[openai]`); the `websockets` dependency is now optional again.
