- Fixed `SarvamTTSService` WebSocket handshakes sending duplicate `User-Agent` headers. Pipecat now sends the Sarvam SDK `User-Agent` via `additional_headers` and disables the default `websockets` client `User-Agent`.