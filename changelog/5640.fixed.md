- Fixed `OpenAIResponsesLLMService` and `OpenAIResponsesHttpLLMService` dropping the context's `tool_choice`, so a turn meant to force or forbid a tool call ran with the API's default. The choice now reaches `responses.create()`, whether set on `LLMContext` or at runtime with `LLMSetToolChoiceFrame`, translated to the shape the Responses API expects for choices that name a tool or restrict the model to a subset of tools.
