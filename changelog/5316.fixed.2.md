- A service that fails to connect is left unable to do its job, so a `ServiceSwitcher` moves off it before the pipeline starts and every frame reaches a service that connected. Connecting happens while the pipeline is set up and is not attempted again, so the service is finished rather than having a bad moment. A failure that doesn't announce itself as permanent, a connection timeout say, previously left the service usable and the switcher on it.
