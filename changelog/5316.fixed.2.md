- Fixed the Daily, Tavus, HeyGen, LemonSlice and Small WebRTC transports setting their shared client up more than once. An input and an output transport share a single client and are now set up concurrently, so both reached the client at the same time and its guard, which read state assigned inside the call it was guarding, let both through. Tavus and LemonSlice built a second Daily client whose callback tasks were left running with nobody to clean them up, Tavus created a second conversation, HeyGen opened a second session, and Small WebRTC connected twice.
