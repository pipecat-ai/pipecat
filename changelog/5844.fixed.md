- Fixed Sarvam TTS errors losing the detail needed to act on them. Errors from both the HTTP and WebSocket surfaces now carry Sarvam's own message along with its error code and request id, so a gating notice or a rejected speaker name reads as the API reported it.
