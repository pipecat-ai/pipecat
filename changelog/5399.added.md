- Added `BusSubscriber.accepts_bus_message(message)`, which the bus consults before every delivery. It accepts everything by default, so a subscriber attached to watch bus traffic sees messages addressed to workers that are asleep, while `BaseWorker` overrides it to take messages only while active.
