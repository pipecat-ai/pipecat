- Added `ErrorFrame.handled`, marking an error that something upstream already recovered from.

    `ServiceSwitcher` sets it when an error triggered a successful failover, so a pipeline that recovered by switching providers isn't also shut down by `on_configuration_error`. The frame still travels upstream, so `on_pipeline_error` handlers observe it as before.
