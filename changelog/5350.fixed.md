- Transports and audio analyzers no longer leave threads behind once a session ends. Each ran its blocking work on a thread pool that was never shut down, so a process opening and closing sessions accumulated threads for its whole life, one per VAD analyzer, turn analyzer, Tk output transport, Vonage client and Daily client, and one per output destination of every transport.
