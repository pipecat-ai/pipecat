- The eval sessions and `EvalSuite.run()` take how a run behaves as one `EvalSessionParams` (`connect_timeout_s`, `default_timeout_ms`, `record_path`, `cache_dir`, `use_cache`, `stop_bot`, `trigger_disconnect`) instead of separate keyword arguments, and the services a run uses (`judge`, `user_tts`, `bot_stt`, and a simulation's `persona_llm`) as keyword arguments on both `from_scenario()` and the constructors.
