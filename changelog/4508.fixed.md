- Fixed `TaskManager.cancel_task` silently suppressing cancellation when called with `asyncio.current_task()` as its argument. `task.cancel()` followed by `await task` on the running task swallowed its own `CancelledError`, so the task body kept running past the cancellation point. Self-cancellation is now detected up front and raises `CancelledError` directly, unwinding the current task immediately.
