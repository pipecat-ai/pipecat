- Fixed the OpenAI, xAI/Grok, and Inworld realtime LLM services double-encoding tool-call results. The result is already a JSON-encoded string by the time it reaches `_send_tool_result`, so calling `json.dumps()` on it again wrapped it in a JSON string literal (and turned the `COMPLETED` completion sentinel into `"COMPLETED"`); results are now passed through unencoded, mirroring the AWS Nova Sonic service.
