- Fixed `GoogleSTTService` losing the utterance in progress when STT settings changed mid-speech. A settings update tore the recognition stream down the moment it arrived, and the audio already sent for that utterance was never transcribed. The reconnect now goes through the shared `STTService` reconnect handling: it waits until the user stops speaking, and audio arriving while the new stream is established is buffered and replayed.
