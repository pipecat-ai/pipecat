- Google STT now applies a settings update by requesting a reconnect rather than tearing the stream down immediately. The reconnect waits until the user stops speaking, so a settings change arriving mid-utterance no longer cancels the RPC under the speaker and loses the audio already sent.
