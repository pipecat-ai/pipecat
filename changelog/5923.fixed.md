- Fixed `LLMContextSummarizer`'s `max_context_tokens` trigger ignoring audio in the context. `LLMContextSummarizationUtil.estimate_context_tokens()` now estimates the WAV audio in `input_audio` parts, such as those added by `LLMContext.add_audio_frames_message()`, from its duration.
