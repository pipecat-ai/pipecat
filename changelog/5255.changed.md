- Changed the default model for `GrokRealtimeLLMService` to `grok-voice-latest`, xAI's recommended Voice Agent alias (currently `grok-voice-think-fast-2.0`). Pin a versioned model explicitly (e.g. `settings=GrokRealtimeLLMService.Settings(model="grok-voice-think-fast-1.0")`) for stability.
