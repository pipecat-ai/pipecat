- Fixed Gemini adapter to also merge `function_response` messages when merging parallel tool calls with thinking enabled. Previously only the model `function_call` turns were merged, leaving response turns split, which caused Gemini to reject requests with `400 Bad Request` due to mismatched call/response counts.
