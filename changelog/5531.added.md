- `CartesiaSTTService.Settings` now exposes Cartesia's `min_volume` and
  `max_silence_duration_secs`, which control the silence that triggers automatic
  transcript finalization on ink-whisper models.
