- Fixed `OpenAIRealtimeSTTService` dropping the speaker's first word when someone is already talking as a session starts. Audio was sent as soon as the WebSocket opened, but the transcription session is only usable once the server has confirmed its configuration, so the leading audio landed on an unconfigured session and was lost or garbled. Audio is now held until the session is configured, then transcribed in full.
