- Removed `AICVADAnalyzer` and `AICFilter.create_vad_analyzer()`, deprecated since 1.4.0. `aic-sdk` 3.0 removed the energy-based VAD they relied on. Use `AICQuailVADAnalyzer`, which runs a dedicated VAD model.
