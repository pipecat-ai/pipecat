- Fixed Deepgram STT settings passed through `extra` being discarded when the key
  matched a declared settings field (for example `extra={"diarize": True}`). Such
  keys are now promoted to their field and sent to Deepgram.
