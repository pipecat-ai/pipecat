- Fixed a Flows issue where a node transition could lose its context and tool updates to a
  concurrent interruption, leaving the LLM advertising the previous node's tools for the rest of
  the call. `LLMMessagesAppendFrame` and `LLMSetToolsFrame` are now `UninterruptibleFrame`s.
