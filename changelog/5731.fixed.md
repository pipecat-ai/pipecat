- Fixed a Flows issue where a node transition could lose its context and tool updates to a
  concurrent interruption, leaving the LLM running the new node with the previous node's prompt
  and tools for the rest of the call. `LLMMessagesAppendFrame`, `LLMMessagesUpdateFrame` and
  `LLMSetToolsFrame` are now `UninterruptibleFrame`s.
