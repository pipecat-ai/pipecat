- Added `AzureOpenAISTTService`, which transcribes audio with Azure OpenAI `/audio/transcriptions` deployments (`gpt-4o-transcribe`, `gpt-4o-mini-transcribe`, `whisper-1`). Set `Settings(model=...)` to your deployment name, and `Settings(model_family=...)` when the deployment isn't named after the model it serves.
