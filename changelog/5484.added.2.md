- Added `MOQParams.audio_out_keepalive` (default on): the MoQ transport keeps the bot's audio track live between utterances with paced silence, so the browser player doesn't clip the start of each utterance during warm-up.
