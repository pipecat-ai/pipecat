- Added `system_prompt_cache_ttl` to `AnthropicLLMService.Settings` to set how long the system prompt's cache entry lasts when prompt caching is enabled: `"5m"` (Anthropic's default) or `"1h"`. A 1-hour entry keeps a system prompt shared by many conversations cached across gaps between them, at a higher cache-write price.
