- Fixed `WordCompletionTracker` trusting a caller-provided `user_facing_text` that carries synthesis tags, which it now strips as it already did for the value it derives from `tts_text`. `AggregatedFrameSequencer` passes the frame's text through raw, so a sentence whose own text contains SSML — a `<break/>` for pacing, `<phoneme>` for a name — put markup in the user-facing channel, making the whole sentence read as one transformed segment. Such a sentence emitted no `AggregatedTextProgressFrame` at all (leaving RTVI protocol 2.x clients with no incremental caption for it), reported the frame complete several words early with the remaining words falling through as unrecognised passthrough, and surfaced the raw tag in `accumulated_text`. A self-closing tag written flush against its neighbours (`"Marcus.<break/>Tell"`) is additionally space-isolated on the TTS cursor and leaves a single space behind on the caption side, so the word boundary it stood in for survives its removal rather than fusing the two words into one token.
