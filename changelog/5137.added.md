- Added `endpointing`, `keywords`, and `format` to `SmallestSTTService.Settings`. `endpointing` finalizes transcripts promptly on trailing silence, `keywords` boosts recognition of domain-specific words/phrases (e.g. `"Blackwell:2,NVIDIA:1"`), and `format` controls whether transcripts get punctuation and capitalization applied.
