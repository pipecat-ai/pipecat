- The `setup_pipeline_task(task)` hook in `PIPECAT_SETUP_FILES` modules has been renamed to `setup_pipeline_worker(worker)`. The legacy name still works but emits a `DeprecationWarning` and will be removed in a future release.
