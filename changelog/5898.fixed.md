- Fixed `SkipTagsAggregator` and `PatternPairAggregator` (with `MatchAction.KEEP`) splitting a sentence at punctuation inside a tag once the tag had closed, and `SkipTagsAggregator` missing a tag that started after an earlier sentence was emitted.
