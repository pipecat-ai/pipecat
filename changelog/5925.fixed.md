- Fixed RTVI `send-text` and explicitly appended user messages not reaching established OpenAI Realtime and AWS Nova Sonic conversations. Deferred messages are delivered on the next context push, and initial context seeding sends each input only once.
