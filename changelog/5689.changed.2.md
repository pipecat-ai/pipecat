- `BackendOutput` no longer carries `is_final`. A backend's final output is its job's response; the outputs before it travel as updates. `transform_output` is called as `transform_output(output, is_final=...)`, and a `BackendReplyStrategy.deliver()` receives `is_final` alongside the output.
