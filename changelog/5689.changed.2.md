- `BackendOutput` no longer carries `is_final`. A backend's answer is its job's response, so no update can pose as it; progress travels as updates. `transform_output` is called as `transform_output(output, is_final=...)`, and a `BackendReplyStrategy.deliver()` receives `is_final` alongside the output.
