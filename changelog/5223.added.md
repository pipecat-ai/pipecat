- Added an `extra_headers` argument to `CartesiaSTTService`, merged into the websocket handshake headers after the defaults so deployments can supply their own authentication or routing headers. `base_url` now also accepts a URL carrying a scheme (`ws://localhost:8000`) rather than only a bare host, so the connection can be made over plain `ws` against a local or proxied endpoint instead of always `wss`.
