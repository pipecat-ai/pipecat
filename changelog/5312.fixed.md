- Fixed `UserBotLatencyObserver` silently dropping turns started by
  `TranscriptionUserTurnStartStrategy` (or any start strategy that bypasses
  VAD). The observer now falls back to `UserStoppedSpeakingFrame` when
  `VADUserStoppedSpeakingFrame` was never received, so `on_latency_measured`
  fires for every turn.
