- `pipecat init` now keeps existing `AGENTS.md`, `CLAUDE.md`, and `GETTING_STARTED.md` files instead of overwriting them on re-run, and points out when a guide was written by an older Pipecat version. Pass the new `--overwrite-guide` flag (renamed from `--force`, now covering all three files) to refresh them.
