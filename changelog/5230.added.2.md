- `AICFilter` and `AICQuailVADAnalyzer` now terminate their ai-coustics
  telemetry sessions explicitly on `stop()` and `cleanup()` instead of leaving
  the close to garbage collection, so sessions end when the pipeline does.
