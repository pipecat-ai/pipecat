- `VADController` and `UserTurnController` now start in a new `start()` that their owner calls, rather than starting themselves from a `StartFrame` or from `setup()`. `VADController.start()` takes no arguments, replacing the private `_start(StartFrame)` whose frame was never read.
