- Fixed `GoogleImageGenService` leaving its `genai.Client` async session open at shutdown. It now closes the session on `stop`, `cancel` and `cleanup`, as `GoogleLLMService` and `GeminiTTSService` do.
