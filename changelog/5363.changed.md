- `AzureLLMService` now defaults `api_version` to `2025-04-01-preview`, the newest dated version Azure serves, so recent Azure features are available without passing `api_version` explicitly. Endpoints on the v1 API surface ignore `api_version` entirely.
