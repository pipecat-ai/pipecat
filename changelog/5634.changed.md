- The `AGENTS.md` that `pipecat init` writes now describes Pipecat Flows as part of Pipecat (`pipecat.flows`) and directs coding agents to build a new flow as a `FlowConfig` YAML file plus a Python tools module, checked with `pipecat flows validate`, reserving Python-built flows for routing or prompts that need code.
