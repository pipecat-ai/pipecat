- Added an `on_pipeline_timeout` event to `PipelineWorker`, called when a frame the worker was waiting on never reaches the end of the pipeline. It fires for a `StartFrame` that never starts the pipeline and for a `CancelFrame` that never drains it, so inspect the frame to tell the two apart.

  ```python
  @worker.event_handler("on_pipeline_timeout")
  async def on_pipeline_timeout(worker, frame):
      if isinstance(frame, StartFrame):
          ...
  ```
