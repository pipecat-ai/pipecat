- The `@tool` decorator's `timeout` argument is renamed to `timeout_secs`, matching `register_function()` / `register_direct_function()` and removing the ambiguity over its unit. `timeout` is still accepted as a deprecated alias (it emits a `DeprecationWarning` and will be removed in a future version); pass `timeout_secs` instead.
