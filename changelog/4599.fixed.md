- Fixed the start of user speech being clipped from transcripts when `OpenAIRealtimeLLMService` is configured for locally-driven turns (server-side turn detection disabled). On barge-in the service clears the input audio buffer, which wiped the speech onset already appended to it; the service now replays a short pre-roll of that audio right after the clear, so the onset is preserved.
