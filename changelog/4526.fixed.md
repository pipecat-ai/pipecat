- Fixed `SarvamSTTService` not setting `TranscriptionFrame.finalized=True` on the terminal transcript. User-turn-stop strategies that fast-path on `finalized=True` (`TurnAnalyzerUserTurnStopStrategy`, `SpeechTimeoutUserTurnStopStrategy`) now correctly trigger immediately on the final transcript instead of falling back to the `SARVAM_TTFS_P99` STT-timeout, removing ~0.87s of latency per user turn at the default `vad.stop_secs=0.3`.
