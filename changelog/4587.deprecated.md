- `PipelineRunner` has been renamed to `WorkerRunner`, since the runner now runs workers (of which `PipelineWorker` is one kind), not just pipelines. `PipelineRunner` still works as a subclass alias but emits a `DeprecationWarning` and will be removed in a future release; import `WorkerRunner` from `pipecat.pipeline.runner` instead.
