- Added `ACSFrameSerializer` for Azure Communication Services Media Streaming WebSocket protocol. Supports audio conversion, DTMF events, interruptions, and automatic call hang-up on `EndFrame`/`CancelFrame`. Install with `pip install pipecat-ai[acs-serializer]`.
