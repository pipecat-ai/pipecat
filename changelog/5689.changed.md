- `BackendLLMWorker` now appends to its LLM's system instruction that it is the backend of a voice assistant, that what it writes goes to the assistant, which decides what the user hears and says it in its own words, and that it should write plain text the assistant can speak from, so a backend prompt need only say what the backend does.
