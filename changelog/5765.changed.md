- `EvalJudge.evaluate_run(criteria, success)` no longer takes a transcript: the judge judges the conversation it keeps, fed with `add_user_message`, `add_assistant_message` and the new `add_tool_call`. `EvalSimulationDriver.transcript()` is gone; the driver feeds the judge as the conversation happens.
