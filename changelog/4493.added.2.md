- New `BusTTSSpeakMessage` bus message: any worker can ask a `PipelineWorker` to speak text via its TTS service. The receiving `PipelineWorker` queues a `TTSSpeakFrame` into its pipeline; pipelines without a TTS service let the frame flow through harmlessly.
