- Deprecated `SpeechmaticsSTTService` `operating_point` on `Settings` and `InputParams`; use `model` instead. Passing it still selects the model and emits a `DeprecationWarning`. It will be removed in 2.0.0.
