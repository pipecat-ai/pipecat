- Fixed Flows leaving its ongoing actions count stuck when the user interrupts during a `tts_say` or `function` action. The frames that signal an action is finished were dropped by the interruption's queue flush, so the task awaiting `execute_actions()` hung and deferred `post_actions` never ran again for the rest of the session. A `function` action's handler raising left the count stuck the same way.
