- Added `saaras:v4` as a supported model for `SarvamSTTService` and made it the default. It follows the same WebSocket contract as `saaras:v3` — same query parameters, event model, and fine-grained VAD tuning knobs — so no runtime logic changed beyond the `MODEL_CONFIGS` entry. Docstrings and error messages that hardcoded "saaras:v3" now reference the capability generically (e.g., "models that support VAD parameters") so the next version doesn't need another pass.
