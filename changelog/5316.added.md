- Added a `start_timeout_secs` parameter to `PipelineWorker`, defaulting to 20 seconds. A processor that blocks while handling the `StartFrame` never lets the pipeline start, and the worker would wait on it forever; it now tears the pipeline down once the timeout elapses, so `run()` returns instead of hanging.
