- Added `AudioVolumeTracker` (`pipecat.audio.volume`), which measures the volume of an audio stream over a rolling 400ms window. Audio is fed in chunks of any size with `update(audio, sample_rate)`, and volume reads 0 until the window holds enough audio to be measurable. `VADAnalyzer` and `RTVIObserver` both track volume through it.
