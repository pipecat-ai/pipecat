- Fixed `GeminiLiveLLMService` crashing with WebSocket error 1007 when seeding a new session with context that contains `FunctionCall` or `FunctionResponse` parts (e.g. after a multi-agent handoff or reconnect). The service now converts tool-call history to text summaries before passing it to `send_client_content`, which only accepts text and media Content.
