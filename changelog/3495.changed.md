- Improved the Time To First Byte (TTFB) measurement for STT services, measuring the time from when the user stops speaking to when the final transcription is received. Added `finalized` field to `TranscriptionFrame` to indicate when a transcript is the final result for an utterance.