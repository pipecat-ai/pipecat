- Added `BaseWhisperSTTService.create_client()`, a public override point for constructing the `AsyncOpenAI` client, matching `BaseOpenAILLMService.create_client()`. It replaces the private `_create_client()`, so subclasses that overrode that method should rename their override.
