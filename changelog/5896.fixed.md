- `LLMAssistantAggregator` now closes an interrupted turn behind the text frames the interruption overtook in its queue, so words the output transport had already played land in the interrupted turn instead of at the front of the next assistant message. The interrupted turn's context frames now follow the `InterruptionFrame` downstream rather than preceding it.
