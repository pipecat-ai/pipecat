- Deprecated the `webrtc` extra's dependency on OpenCV. `opencv-python-headless` will be removed from the `webrtc` extra in 2.0.0. Video pipelines using `SmallWebRTCTransport` should start installing the new `webrtc-video` extra (`pipecat-ai[webrtc-video]`) instead.
