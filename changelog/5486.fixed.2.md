- `speech_activity_period` on `VADController` and `VADProcessor` now takes effect. It was ignored, so `UserSpeakingFrame` was broadcast on every input audio chunk, 50 times a second on a 20 ms transport, instead of at most once per period.
