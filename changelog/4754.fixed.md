- Fixed `SarvamHttpTTSService` not recomputing model-dependent settings (pace range, preprocessing, pitch/loudness/temperature support) when the `model` is changed via `TTSUpdateSettingsFrame` at runtime.
