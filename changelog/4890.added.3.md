- Added an `auto_start_recording` option to `AudioBufferProcessor`. When enabled, recording starts automatically as soon as the pipeline starts, without requiring a `start_recording()` call or an `AudioBufferStartRecordingFrame`. Bots generated by the Pipecat CLI with the recording feature now use this option instead of starting recording from connection event handlers.
