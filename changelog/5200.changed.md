- `GrokRealtimeLLMService`'s `voice` setting is now typed `str` instead of a pinned `Literal` of five names. The `Literal` validated nothing — it was unioned with `str`, which collapses to `str` — while still needing a release every time xAI ships a voice. `GrokVoice` stays as an alias of `str` so existing imports keep working, and the docstrings now point at xAI's docs instead of duplicating a name list that had gone stale in three places.
