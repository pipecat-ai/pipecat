Fixed `AggregatedFrameSequencer.force_complete` emitting the unspoken remainder only as a `TTSTextFrame`: a slot that spoke at least one word now also advances the tracker's user-facing cursor and emits a final `AggregatedTextProgressFrame` covering its full user-facing text, so transcript consumers no longer lose the remainder when word timestamps stop mid-slot. A slot that never spoke still contributes nothing.
