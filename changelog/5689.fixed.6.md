- Fixed a tool result asking for inference being dropped when another result was queued behind it and that one asked for none: the assistant aggregator now runs inference once the burst is in, whatever the last result asks for. The end of the user's turn also clears a push owed for a result that landed while they spoke, which would otherwise have run inference again on the next result that asked for none.
- Fixed Inworld Realtime sending a user's message twice when a function call result arrived between the server's VAD turn and the aggregator writing that message.
