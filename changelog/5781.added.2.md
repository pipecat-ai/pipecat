- `GeminiLiveLLMService` holds the bot turn open while Gemini reports `interaction_status: IN_PROGRESS`, so a reply that spans several `turn_complete` messages is recorded as a single assistant turn. Requires `google-genai>=2.18.0` (now the `google` extra's floor); older SDKs log a warning.
