- Fixed `GladiaSTTService` tearing down and reconnecting its websocket when Gladia reports a translation addon failure, and silently ignoring rejected audio chunks. Both are now reported upstream as non-fatal `ErrorFrame`s while transcription continues on the same connection.
