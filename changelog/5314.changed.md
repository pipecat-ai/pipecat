- Changed `function_call` matching in `pipecat.evals` so a call of the expected name whose arguments don't match no longer ends the match. It is set aside like a name mismatch and the turn keeps looking, so a call the LLM corrects and repeats satisfies the expectation; a turn where none match reports the arguments that did arrive rather than reporting the call as never made.
