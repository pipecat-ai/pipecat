- `BaseUIWorker` is deprecated. `UIWorker` now reports its job groups to the client itself, so a bot dispatches from one of its `@job` handlers where it used a `BaseUIWorker` as a dispatcher. Will be removed in 2.0.0.
