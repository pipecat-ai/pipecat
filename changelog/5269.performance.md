- `pipecat eval suite` now shares the local voice and transcriber its runs need,
  pooled by config, instead of building a fresh pair for every run. A long sweep
  no longer grows in memory until it exhausts the machine. A scenario asking for
  a different speech or transcription model still gets that model.
