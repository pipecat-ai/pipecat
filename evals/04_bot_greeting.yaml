name: bot_greets_then_describes_capabilities

judge:
  service: ollama
  model: llama3:latest

turns:
  - expect:
      - event: llm_response
        eval: "greets the user and offers to help"

  - user: "what can you do?"
    expect:
      - event: llm_response
        eval: "describes the agent's capabilities clearly and concisely"
